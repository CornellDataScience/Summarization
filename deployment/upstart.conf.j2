description "upstart"

start on (filesystem)
stop on runlevel [016]

respawn
setuid root
setgid nogroup
chdir {{ repository_path }}

exec sudo gunicorn app:app --bind=127.0.0.1:5000 --workers 3 --user=root --log-level=debug